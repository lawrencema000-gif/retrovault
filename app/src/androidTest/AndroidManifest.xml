<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Runs in the instrumentation (target) process, unlike EmulatorActivity (:emu),
             so tests can drive LibretroBridge and read its stats directly. -->
        <activity
            android:name="com.retrovault.app.RenderTestActivity"
            android:exported="false"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />
    </application>
</manifest>
