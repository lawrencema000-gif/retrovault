package com.retrovault.billing

/**
 * Features unlocked by Pulsar Gold. These gate convenience + hosted features only — the emulator,
 * the legal catalog, and the GPL source always stay free.
 */
enum class GoldFeature {
    NO_ADS,
    CLOUD_SYNC,
    EXTRA_SAVE_SLOTS,
    OVERLAY_SKINS,
}
